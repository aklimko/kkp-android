<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:title="@string/services">
        <menu>
            <item
                    android:id="@+id/nav_bought"
                    android:icon="@drawable/ic_bought"
                    android:checked="true"
                    android:title="@string/bought_products"/>
            <item
                    android:id="@+id/nav_cleaned"
                    android:icon="@drawable/ic_cleaned"
                    android:title="@string/cleaned_rooms"/>
            <item
                    android:id="@+id/nav_history"
                    android:icon="@drawable/ic_history"
                    android:title="@string/history"/>
        </menu>
    </item>

    <item android:title="@string/account">
        <menu>
            <item
                    android:id="@+id/nav_settings"
                    android:icon="@drawable/ic_settings"
                    android:title="@string/settings"/>
            <item
                    android:id="@+id/nav_logout"
                    android:icon="@drawable/ic_logout"
                    android:title="@string/logout"/>
        </menu>
    </item>

</menu>
